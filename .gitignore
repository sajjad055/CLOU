# Dependencies
node_modules/
.pnpm-store/

# Build output (GitHub Actions rebuilds this on every push)
dist/

# Local dev / editor
.DS_Store
*.local
.vscode/*
!.vscode/extensions.json

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Tooling skill (contains its own nested git repo — not part of the prototype)
.kiro/skills/
